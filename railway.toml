[build]
builder = "DOCKERFILE"
dockerfilePath = "tt-policy-tracker/Dockerfile"
watchPatterns = ["tt-policy-tracker/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
